# ── Local Database (XAMPP) ─────────────────────────────────────────────────────
LOCAL_DB_HOST=localhost
LOCAL_DB_USER=root
LOCAL_DB_PASS=
LOCAL_DB_NAME=portfolio

# ── Live Database (Production) ────────────────────────────────────────────────
LIVE_DB_HOST=
LIVE_DB_USER=
LIVE_DB_PASS=
LIVE_DB_NAME=

# ── Admin Credentials ─────────────────────────────────────────────────────────
# ADMIN_PASS should be a bcrypt hash. Generate one with:
#   php -r "echo password_hash('YourPassword', PASSWORD_DEFAULT);"
ADMIN_USER=admin
ADMIN_PASS=$2y$10$REPLACE_WITH_YOUR_BCRYPT_HASH
